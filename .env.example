# ================================================
# VARIABLES DE ENTORNO - WhatsApp API
# ================================================

# MySQL REMOTO Configuration
DB_HOST=tu-servidor-mysql.com           # Ejemplo: mysql.tudominio.com
DB_PORT=3306
DB_USER=whatsapp_user
DB_PASSWORD=tu_password_mysql_muy_seguro_aqui
DB_NAME=whatsapp_api_multitenant

# Domain Configuration (para SSL)
DOMAIN=api.tudominio.com               # Tu dominio/subdominio
EMAIL=tu-email@dominio.com             # Email para Let's Encrypt

# Node.js Configuration
NODE_ENV=production
PORT=3000

# ================================================
# INSTRUCCIONES:
# 1. Copia este archivo: cp .env.example .env
# 2. Edita .env con tus valores reales
# 3. NUNCA subas .env a git
# 4. Asegúrate que tu MySQL remoto permita conexiones desde tu servidor
# ================================================
